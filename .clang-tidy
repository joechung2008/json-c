---
# Conservative, C-focused clang-tidy configuration for json-c
Checks: >
  clang-analyzer-*,
  bugprone-*,
  performance-*,
  portability-*,
  readability-*,
  -readability-magic-numbers,
  -clang-analyzer-alpha.*,   # experimental analyzer checks — may be noisy
  -modernize-*,              # C project — disable C++ modernize checks
  -hicpp-*,                 
  -bugprone-macro-parentheses

HeaderFilterRegex: 'src/|include/'
WarningsAsErrors: ''
FormatStyle: file
CheckOptions:
  - key:             readability-identifier-naming.VariableCase
    value:           lower_case
  - key:             portability-signed-char.WarnOnSignedChar
    value:           true

# You can selectively disable additional checks if they prove noisy.
# Use NOLINT comments for local exceptions when necessary.
